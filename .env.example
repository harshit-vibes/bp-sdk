# Lyzr Agent IDs (required for core functionality)
# These are the agent IDs from your Lyzr Studio account
ARCHITECT_AGENT_ID=your-architect-agent-id
CRAFTER_AGENT_ID=your-crafter-agent-id

# Optional: Additional agents for enhanced features
# REPLY_SUGGESTER_AGENT_ID=your-reply-suggester-agent-id
# IDEA_SUGGESTER_AGENT_ID=your-idea-suggester-agent-id

# Note: User credentials (API key, bearer token, org ID) are entered
# via the UI and stored in browser cookies, not environment variables.
